contract:
  contract_id: TOS-CTR-022
  contract_type: Task or Vertical Slice
  title: <Task or Vertical Slice title>
  version: 0.1.0
  status: draft
  applicability: core
  not_applicable_reason: <required when not_applicable>
  owner_function: Project Manager
  approver: <approval authority>
  applies_to:
  - <project/component/domain/task/etc.>
  effective_date: <YYYY-MM-DD>
  review_due: <YYYY-MM-DD or event>
  supersedes: []
  source_of_truth: <canonical file path or system>
definition:
  purpose: Defines one bounded outcome with contracts, scope, tests, evidence, and rollback.
  scope: <included scope>
  out_of_scope:
  - <explicit exclusions>
  terms:
    <term>: <definition>
  trigger: Every executable unit of work
obligations:
  inputs:
  - <required input>
  outputs:
  - <required output>
  preconditions:
  - <must be true before execution>
  invariants:
  - <must always remain true>
  postconditions:
  - <must be true after execution>
  permissions:
  - <allowed action>
  prohibitions:
  - <forbidden action>
  failure_behavior:
  - <required failure behavior>
assurance:
  acceptance_criteria:
  - <observable pass/fail condition>
  validation:
  - <command/check/review>
  negative_controls:
  - <deliberate violation that must be detected>
  required_evidence:
  - <evidence class and artifact>
change_and_exit:
  change_control: <decision/change record required>
  compatibility: <compatibility rule>
  deprecation: <deprecation procedure>
  retirement: <retirement and archive procedure>
contract_body:
  outcome: <define outcome>
  in_scope: <define in scope>
  out_of_scope: <define out of scope>
  binding_contracts: <define binding contracts>
  acceptance: <define acceptance>
  validation: <define validation>
  rollback: <define rollback>
