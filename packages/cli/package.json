{
  "name": "@topshelf-os/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "tos": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@topshelf-os/kernel": "workspace:*",
    "@topshelf-os/shared": "workspace:*"
  }
}
