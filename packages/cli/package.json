{
  "name": "@topshelf-os/cli",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "bin": {
    "tos": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@topshelf-os/contracts": "workspace:*",
    "@topshelf-os/kernel": "workspace:*",
    "@topshelf-os/shared": "workspace:*",
    "yaml": "^2.8.0"
  }
}
